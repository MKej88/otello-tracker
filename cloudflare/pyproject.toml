[project]
name = "otello-tracker-cloudflare"
version = "0.11.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.141.1",
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
